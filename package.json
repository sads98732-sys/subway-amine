{
  "name": "veilspire",
  "version": "0.1.0",
  "description": "Veilspire — an original open-world magical action RPG built with Three.js",
  "author": "Lunaneco",
  "private": true,
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/unit/*.test.js",
    "check": "npm test && npm run build",
    "verify": "node scripts/verify.mjs",
    "verify:browser": "node scripts/browser-launch.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^8.1.5"
  }
}
